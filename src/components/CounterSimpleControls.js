import { useDispatch } from "react-redux"

const  CounterSimpleControls = () => {
    const dispatch = useDispatch();

     function incrementBy1Call(){
         dispatch({type})
     }
     "increment_by_1"
}